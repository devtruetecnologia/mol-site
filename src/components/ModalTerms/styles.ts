import styled from 'styled-components';

export const Container = styled.main`
  .checkbox-terms {
    p {
      font-size: 1.2rem;
    }

    .chakra-checkbox__control {
      width: 1.5rem;
      height: 1.5rem;
    }
  }
`;